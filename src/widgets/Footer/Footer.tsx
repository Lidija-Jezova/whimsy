import styles from './Footer.module.scss'
import { classNames } from 'shared/lib/classNames/classNames'

const Footer = () => <footer className={classNames(styles.Footer)}>footer</footer>

export default Footer
